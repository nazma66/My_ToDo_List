<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.My_ToDo_List" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize the color of the overflow menu icon (three dots) -->
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <!-- Customize the color of the text in the overflow menu -->
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Theme.My_ToDo_List" parent="Base.Theme.My_ToDo_List" />

</resources>